import type { FileDetails } from 'bundlemon-utils';
import type { NormalizedFileConfig } from '../types';
interface GetFilesDetailsParams {
    baseDir: string;
    config: NormalizedFileConfig[];
    allFiles: string[];
    stopOnMatch: boolean;
}
export declare function getFilesDetails({ baseDir, config, allFiles, stopOnMatch, }: GetFilesDetailsParams): Promise<FileDetails[]>;
export declare function groupFilesByPattern(files: FileDetails[]): FileDetails[];
export {};
